# @setu/app

The Setu PWA (React + TypeScript + Vite). See the [repo root README](../README.md)
for the actual quickstart, offline verification steps, and deploy
instructions — this package is one third of that.

```bash
npm run dev       # from repo root: http://localhost:5173
npm run build     # -> app/dist
```
